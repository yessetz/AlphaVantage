name: python-script

on: [push]

jobs:
  run-python-script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: pip install -r requirements.txt

    - name: Run Python script
      working-directory: scripts/
      run: python getVantageDataFunctions.py

    - name: Commit and push changes
      env:
        GITHUB_TOKEN: ${{ secrets.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 }}
      run: |
        git config --global user.name 'yessetz'
        git config --global user.email 'yessetkhan@gmail.com'
        git add .
        git commit -m "feat: update output file"
        git push

name: python-script

on: [push]

jobs:
  run-python-script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: pip install -r requirements.txt

    - name: Run Python script
      working-directory: scripts/
      run: python getVantageDataFunctions.py

    - name: Commit and push changes
      env:
        GITHUB_TOKEN: ${{ secrets.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 }}
      run: |
        git config --global user.name 'yessetkhan@gmail.com'
        git config --global user.email 'yessetkhan@gmail.com'
        git add .
        git commit -m "feat: update output file"
        git push
